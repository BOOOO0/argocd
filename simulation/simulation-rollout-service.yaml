apiVersion: v1
kind: Service
metadata:
  name: rollout-dp-service-1
  namespace: rollout-simulation
  labels:
    app: rollout-dp-1
spec:
  ports:
    - port: 12345
      protocol: TCP
      targetPort: 3100
  selector:
    app: rollout-dp-1

