apiVersion: v1
kind: Service
metadata:
  name: prometheus-service-1
  namespace: simulation
  labels:
    app: rollout-dp-1
spec:
  ports:
    - port: 12345
      protocol: TCP
      targetPort: 3100
  selector:
    app: rollout-dp-1

